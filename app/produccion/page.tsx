import SectionPlaceholder from '@/app/components/sections/sectionPlaceholder';

export default function ProduccionPage() {
  return (
    <SectionPlaceholder
      title="Produccion"
      description="Esta seccion queda preparada para conectar el flujo operativo de produccion sin afectar la estructura principal."
    />
  );
}
